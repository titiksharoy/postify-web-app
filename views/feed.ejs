<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feed</title>

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-zinc-900">

    <div class="min-h-screen flex items-center justify-center p-6">

        <div class="w-full max-w-3xl bg-zinc-800 p-10 rounded-3xl shadow-2xl">

            <div class="flex justify-between items-center mb-8">
                    
                <div>
                 <h1 class="text-4xl font-extrabold bg-gradient-to-r from-blue-400 to-pink-400 bg-clip-text text-transparent">
    Postify
</h1>
                    <p class="text-zinc-300 mt-5 text-lg font-medium">
    Welcome back, <span class="text-white"><%= user.name %></span>!
</p>
                </div>
    <div class="flex items-center gap-3">

    <a
        href="/profile"
        class="bg-blue-500 hover:bg-blue-600 text-white px-5 py-2 rounded-xl transition"
    >
        Profile
    </a>

    <a
        href="/logout"
        class="bg-red-500 hover:bg-red-700 active:scale-95 text-white font-medium px-5 py-2 rounded-xl transition-all duration-200 shadow-lg"
    >
         Logout
    </a>

</div>

            </div>
        <div class="w-43 h-45 bg-zinc-500 rounded-3xl overflow-hidden">
            <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="Profile Picture">
         </div>
        
            
            <div class="mt-20 mb-4">
                <h3 class="text-blue-400 text-lg"> Your Corner of the Internet ✨</h3>
              

              
                          <div class="postcontainer mt-5">
                            <% posts.reverse().forEach(function(post){ %>

                                
                  <div class="bg-[#334155] p-10 rounded-2xl mb-4">
                    <div class="flex items-center gap-3 mb-4">

        <img src="/images/uploads/<%= post.user.profilepic %>" class="w-12 h-12 rounded-3xl object-cover">
        <div> <h4 class="text-white font-semibold">
             <%= post.user.name %>
                 </h4>
     <p class="text-gray-400 text-sm"> @<%= post.user.username %> </p>
           </div>

         </div>
                <p class="text-gray-200 text-lg leading-relaxed">
                    <%= post.content %>  
                                   </p>
                                <% if(post.image){ %>

    <img
        src="/images/uploads/<%= post.image %>"
         class="w-full max-w-lg mx-auto mt-5 rounded-xl object-cover shadow-md"
    >

<% } %>
<p class="text-xs text-gray-400 mt-3">
    <%= post.date.toLocaleString("en-IN", {
        day: "numeric",
        month: "short",
        year: "numeric",
        hour: "numeric",
        minute: "2-digit"
    }) %>
</p>

                    <small class="mt-2 text-black inline-block"> <%= post.likes.length %> likes</small>
                     <!--like and comment buttons  -->
                  <div class="flex items-center gap-4 mt-4">

    <a href="/like/<%= post._id %>?from=feed"
    class="bg-pink-500 hover:bg-pink-600 text-white px-5 py-2 rounded-xl transition duration-200 shadow-md inline-block">

        <%= post.likes.indexOf(user._id) == -1 ? "Like ❤️" : "Unlike ❤️" %>

    </a>
 <% if(post.user._id.equals(user._id)){ %>

    <a  class="text-yellow-400 hover:text-yellow-300"
        href="/edit/<%= post._id %>" >  Edit </a>
         <% } %>
          <% if(post.user._id.equals(user._id)){ %>

    <a href="/deletepost/<%= post._id %>?from=feed"
    onclick="return confirm('Delete this post?')"
   class="text-red-400 hover:text-red-300">
    Delete
</a>
         <% } %>
</div>

<form action="/comment/<%= post._id %>?from=feed" method="POST" class="flex gap-2 mt-4">

    <input
        type="text"
        name="comment"
        placeholder="Write a comment"
        class="flex-1 px-4 py-2 rounded-xl bg-zinc-700 text-white outline-none"
    >

    <button 
        type="submit"
        class="bg-blue-500 hover:bg-blue-600 text-white px-5 py-2 rounded-xl transition duration-200 shadow-md"
    >
        💬 Comment
    </button>

</form>

<div class="mt-4 space-y-2">

    <% post.comments.forEach(function(comment){ %>

       <div class="bg-zinc-700 px-3 py-2 rounded-lg flex justify-between items-center">

    <p class="text-white text-sm break-words">
        <%= comment.text %>
    </p>
        <% if(comment.user.equals(user._id)){ %>
    <a href="/deletecomment/<%= post._id %>/<%= comment._id %>?from=feed"
        class="text-red-400 hover:text-red-300 text-sm ml-4 whitespace-nowrap"
    >
        Delete
    </a>
    <% } %>
</div>
    <% }) %>

</div>
               </div>
                          <%  })%>
                          </div>

                 </div>
           </div>


     </div>

</div>

</body>
</html>


